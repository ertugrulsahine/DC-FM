{
  "name": "dc-fm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/model/*.test.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
